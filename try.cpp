#include<iostream>
#include<vector>
using namespace std;

int main(){
      vector <int> v;

      for(int i=0; i<v.size(); i++){
        int element;
        cin>>element;
        push_back(element);
      }
    return 0;
}